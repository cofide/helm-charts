apiVersion: v1
kind: ConfigMap
metadata:
  name: cofide-connect
data:
{{- range $key, $value := .Values.connect.env }}
  {{ $key }}: {{ $value | quote }}
{{- end }}
  CONNECT_TRUST_DOMAIN: {{ .Values.connect.trustDomain }}

