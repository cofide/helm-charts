apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cofide-connect.fullname" . }}-test-connection"
  labels:
    {{- include "cofide-connect.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cofide-connect.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
