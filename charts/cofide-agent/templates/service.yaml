apiVersion: v1
kind: Service
metadata:
  name: {{ include "cofide-agent.fullname" . }}
  labels:
    {{- include "cofide-agent.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cofide-agent.selectorLabels" . | nindent 4 }}
