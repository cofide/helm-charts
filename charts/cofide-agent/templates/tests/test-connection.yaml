apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cofide-agent.fullname" . }}-test-connection"
  labels:
    {{- include "cofide-agent.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cofide-agent.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
